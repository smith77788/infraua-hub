# Відкриті знахідки

Проблеми, знайдені, але ще не виправлені. Один рядок — одна знахідка.
Стан міняється в тому ж коміті, що й саме виправлення (див. `AGENTS.md`, §3).

Стани: `відкрито` · `в роботі (хто)` · `виправлено (коміт)` · `відхилено (чому)`

| # | Знахідка | Де | Стан |
|---|---|---|---|
| 1 | Верхня панель обрізана на вузькому екрані: «Оновити», «Брифінг», «Про платформу» виходять за межу й недосяжні | `src/routes/index.tsx` (шапка) | виправлено: перевірено в браузері на 320/360/390/768/1280 px — жодна кнопка не виходить за межу екрана |
| 2 | Підпис карти «Leaflet \| Tiles © Esri» перекриває плеєр часу на телефоні | `src/styles.css`, шар підпису Leaflet | виправлено (підпис піднято над плеєром, контроли Leaflet у темі консолі) |
| 3 | Кількість підстанцій 110 кВ+ по країні | `src/lib/infra.functions.ts` | виправлено: заміряно з розгортання — **4109** (3 вузли, 3937 ліній, 169 відношень). Причиною відмов Overpass був відсутній `User-Agent`, а не недоступність |
| 4 | Розходження гідратації: час брався під час першого малювання | `TimelinePlayer`, `routes/brief.tsx` | виправлено (b8101f0) |
| 5 | Вигадана адреса `hello@infraua.org` на сторінці «Про платформу» | `src/routes/about.tsx` | виправлено (b8101f0) |
| 6 | `vitest` падає на всіх файлах — не той рушій, а не поламані тести | README | виправлено (b8101f0) |
| 7 | Опорні записи мали `source` виду `openstreetmap.org/#map=…` — вид карти, а не запис; у інтерфейсі не відрізнялися від перевірених | `infra-seed.ts`, інспектор | виправлено (поле `origin`, кнопка «Джерело» лише за наявності запису) |
| 8 | Запити до Overpass ішли без `User-Agent`, що порушує політику OSM; відмови (HTTP 406) читалися як «джерело лежить» | `src/lib/infra.functions.ts` | виправлено (A/B з Railway: 406 → валідний JSON) |
| 9 | З цього середовища розробки Overpass недосяжний на рівні зʼєднання (HTTP 000) — заміри з ним робити лише з розгортання | середовище | відкрито |
| 10 | `platform-api` на Railway не має тригера автодеплою: `deploymentTriggerCreate` недоступний project-токену. Через це новий код платформи діставався продакшну лише руками, і `/admin` у боті впирався в стару платформу | Railway / CI | виправлено: `.github/workflows/deploy-platform.yml` деплоїть платформу з main через `railway up` (на зміни в `platform/**`). Потрібен разовий крок власника: секрет `RAILWAY_TOKEN` у репозиторії + на сервісі Dockerfile Path = `Dockerfile.platform`. Альтернатива без CI — підключити репо+гілку main до сервісу в дашборді Railway |
| 11 | Дані димового тесту в графі платформи | `platform-api` | виправлено: додано відкликання партії (`POST /api/platform/retract`), партію `smoke` відкликано, граф порожній, дію записано в аудит |
| 12 | `AuditLog` тримав весь журнал у памʼяті; з аудитом читання він росте разом із трафіком | `platform/core/audit/AuditLog.ts` | виправлено: у памʼяті лишається покажчик (зсув + інтерновані актор/дія), записи читаються з диска. Заміряно на 100 000 записів (32.6 МБ): утримувана памʼять 90.0 → 11.9 МБ; ціна — старт 185 → 255 мс, `verify` 336 → 599 мс |
| 13 | П'ять пакетів (`leaflet`, `react-leaflet`, `@react-leaflet/core`, `@types/leaflet`, `@types/geojson`) закріплені в `bun.lock` на приватне дзеркало Lovable `europe-west1-npm.pkg.dev`, недосяжне з середовища розробки Claude Code | `bun.lock` | обхід: ті самі тарболи з `registry.npmjs.org` — sha512 збігається з lock-файлом побайтово, перевірено |
| 14 | `findDuplicateCandidates` порівнював кожну пару в межах типу на кожен запит `/analytics`: 500 вузлів — 1.8 с, 1000 — 7.6 с, 2000 — 29 с, 4000 — 116 с | `platform/core/analytics/EntityResolver.ts` | виправлено: блокування перед порівнянням, 4000 вузлів — 0.2 с; тест доводить збіг із повним перебором |
| 15 | `VectorIndex.search` перевекторизовував увесь корпус на кожен запит: 5000 документів, 20 запитів — 913 мс | `platform/core/vector/VectorIndex.ts` | виправлено: інвертований індекс і збережені норми, ті самі 913 → 95 мс; тест доводить побітову рівність ранжування |
| 16 | Список корпоративних суфіксів у `normalizeLabel` — лише латиниця, а `\b` у JS визначено через ASCII `\w`, тож кириличний суфікс не міг би зіставитися навіть після додавання | `platform/core/analytics/EntityResolver.ts` | виправлено: фільтрація цілих токенів, додано українські й російські правові форми |
| 17 | Токенізатор пошуку — `[a-zа-яё0-9]`, тобто російська абетка: і, ї, є, ґ ставали роздільниками. «Київобленерго» індексувалося як «ки»+«вобленерго», «Дніпро» — як «дн»+«про». Той самий клас помилки, що виправлений у `slugify` | `platform/core/vector/VectorIndex.ts` | виправлено: `\p{L}\p{N}` з прапорцем `u`, додано українські стоп-слова |
| 18 | На розрідженому графі обидва структурні сигнали ризику вироджувалися: у лісі мостом є кожне ребро, а посередництво нормується на максимум у знімку. Заміряно на 100 справжніх тендерах: 138 зі 164 сутностей мали бали, кілька — «severe» | `platform/core/analytics/RiskScorer.ts`, `GraphMetrics.ts` | виправлено: `minSide` для мосту (розмір меншої сторони), `minRaw` для посередництва (абсолютна кількість шляхів) |
| 19 | Половина онтології (`AWARDED_CONTRACT`, `HIDDEN_BENEFICIARY_OF`) і найважчий сигнал ризику (`self_dealing_pattern`, вага 35) не мали жодного джерела даних | `platform/config/` | виправлено: Prozorro (договори) + ЄДР (засновники, задекларовані бенефіціари, керівники). `HIDDEN_BENEFICIARY_OF` **навмисно** лишився без джерела: реєстр тримає розкрите володіння, і підключати його до «прихованого» означало б оцінювати законослухняність як приховування |
| 20 | Bun `fetch` через проксі середовища розробки падає з `ECONNRESET` на `public.api.openprocurement.org`; `curl` і Python до того самого хоста працюють | середовище | відкрито — живі заміри робилися через Python, фікстура зі справжніх записів у `platform/tests/fixtures/` |
| 21 | Відтворення минулого стану програвало весь журнал ревізій | `platform/core/graph/RevisionLog.ts` | виправлено: періодичні знімки з перевіркою на окупність. На робочому навантаженні (щоденне оновлення 4109 обʼєктів, 200 000 ревізій) відтворення голови 795 → **15 мс**, знімків 5.0 МБ проти журналу 44.7 МБ |
| 22 | Відмітка часу не розрізняє дві зміни в одну мілісекунду, а завантаження робить їх сотнями | `platform/core/graph/` | виправлено: курсор послідовності (`asOfSeq`, `revisionHead`); відмітка часу лишилася для людських запитів |
| 23 | Наслідки відмови рахуються в обʼєктах і мегаватах, але не в людях. Потрібне населення: теги `place=*` з `population` в OSM дають його по населених пунктах | `src/lib/contingency.ts` | відкрито — Overpass із середовища розробки недосяжний (№9), тож підключати й міряти це треба з розгортання |
| 24 | Перебір епіцентрів бере кандидатами самі обʼєкти набору: епіцентр між двома обʼєктами, що зачепив би обидва, не зʼявиться | `src/lib/contingency.ts` | відкрито — свідома межа методу, названа в коді; рівномірна сітка по країні витратила б більшість роботи на порожні квадрати |
| 25 | Ні Node, ні Bun не декодують windows-1251, у якому публікується ЄДР; `TextDecoder` кидає `ERR_ENCODING_NOT_SUPPORTED`, а обхід через UTF-8 не помиляється й дає кашу | `platform/core/ingestion/EdrConnector.ts` | виправлено: власний декодер, таблиця згенерована з еталонного кодека і звірена в тестах |
| 26 | `self_dealing_pattern` вимагає обох звʼязків на **одному** вузлі, а справжній візерунок конфлікту інтересів — це шлях: особа є бенефіціаром постачальника і керівником замовника, який йому присудив | `platform/config/risk_signals.json` | виправлено: `core/analytics/ConflictOfInterest.ts` + `GET /api/platform/analytics/conflicts`. Сам сигнал лишено для випадку, коли обидві ролі тримає організація, і межу названо прямо в його `reason` |
| 27 | Тест стверджував, що `new TextDecoder('windows-1251')` кидає — це властивість рантайму, а не коду. Локально Bun 1.3.11 кидає, у CI `bun-version: latest` уже вміє, і CI впав на `main`, поламавши й коміти паралельної сесії | `platform/tests/integration/edr.test.ts` | виправлено: перевіряється поведінка конектора, а не рантайму; звірка з платформним декодером робиться там, де він є, і пропускається, де немає |
| 28 | Таблиця cp1251 бралася з кодека Python, який вважає байт 0x98 невизначеним, тоді як WHATWG (і будь-який `TextDecoder`) відображає його в U+0098 — звірка двох декодерів впала б на новому рантаймі | `platform/core/ingestion/EdrConnector.ts` | виправлено: таблиця з індексу WHATWG, звірено побайтово |
| 29 | Стан джерел у консолі бачить лише поточне звернення: джерело, що віддає 200 із третиною звичних рядків, не «лежить», але зсуває кожне похідне число | `src/lib/sources.ts` | частково: у платформі зʼявилися знімки партій із вироком (`platform/core/lineage/`). Консоль ще їх не читає — це вимагає рішення щодо подання |
| 30 | `RevisionLog` тримав усі ревізії в памʼяті — та сама проблема, що була у №12 | `platform/core/graph/RevisionLog.ts` | виправлено: покажчик замість обʼєктів. Заміряно на 200 000 ревізій (40 МБ): утримувана памʼять 78.4 → **11.1 МБ**, старт 468 → 617 мс |
| 31 | Знімки ревізій окупаються лише тоді, коли ревізії повторно чіпають ті самі сутності. На завантаженні, де кожна ревізія створює нову сутність, знімки займали 226 МБ проти 45 МБ журналу і вчетверо сповільнювали завантаження | `platform/core/graph/RevisionLog.ts` | виправлено: рішення про знімок ухвалюється на кожній межі за співвідношенням «розмір стану проти ревізій, які знімок замінює» — за лічильниками, без серіалізації |
| 32 | Опорний перелік 90 ключових обʼєктів інфраструктури (`infra-seed.ts`) імпортувався статично з клієнтського маршруту заради миттєвого показу — тобто їхав у бандлі до кожного відвідувача незалежно від запитів і від будь-якого вимикача | `src/routes/index.tsx`, `src/lib/infra-seed.ts` | виправлено: динамічний імпорт усередині серверного обробника. Заміряно на збірці: чанк маршрутів 92 647 → **76 265 байт**, згадок «Укренерго» в `.output/public` 11 → **0** |
| 33 | Вимикач шарів не вмикався назад: `disabled` проставлявся лише на відмові, тож увімкнений шлях був невідрізненний від «відповіді ще немає», яке консоль свідомо трактує як вимкнено | `src/lib/infra.functions.ts` | виправлено: `disabled` проставляється обома значеннями явно, читання винесено в `infraLayersOff()` з тестами. Зловлено перевіркою в браузері, не міркуванням |
| 34 | Похідні модулі (`infra-criticality`, `contingency`, `power-grid`, `threat-correlation`) лишилися в клієнтському бандлі, хоча за вимкненими шарами рахують по порожньому набору | `src/lib/` | відкрито — це код без даних, тож ризику не несе; прибирати його варто разом із рішенням, чи шари повертаються взагалі |
| 35 | Вимикач шарів був лише змінною оточення, тож керувати ним із бота неможливо: Worker не може переписати власне оточення | `src/lib/infra-gate.ts` | виправлено: вимикач із двох частин — дозвіл у змінній, ручка на диску платформи; шари віддаються лише коли обидві відкриті, вимкнути можна кожною окремо |
| 36 | Реєстр джерел вирішував про фіди інфраструктури при завантаженні, тож кожен оберт ручки вимагав би рестарту платформи | `platform/core/ingestion/declarative/SourceRegistry.ts` | виправлено: рішення ухвалюється в момент запиту (`list`/`get`), `all()` лишився для чистки. Перевірено на працюючому сервісі — фіди зникають і повертаються без рестарту |
| 37 | Консоль без платформи керується лише змінною оточення: ручки з бота там немає | `src/lib/infra-gate.ts` | свідома межа — розгортання без платформи штатне, і для нього вимикач лишився тим, чим був. Недоступна платформа читається як «вимкнено» |
| 38 | Підписки персонального радара лежать у JSON-файлі процесу. Без постійного тому кожен редеплой стирає всіх підписників — і тихо: лічильник просто починається з нуля | `src/lib/subscriber-store.ts` | частково: межа названа в коді й у `.env.example`, `/stats` у боті показує `durable: true/false` і підказує задати `BOT_DATA_DIR`. Сам том має підключити власник — з коду це неможливо |
| 39 | Inline-режим бота вмикається лише вручну в @BotFather (`/setinline`); Bot API методу для цього не має. Поки не ввімкнено, Telegram не шле `inline_query`, і код про це не дізнається — виглядає як «не працює» | @BotFather / розгортання | відкрито — разовий крок власника, описаний у `.env.example` |
| 40 | Дедуп персональних сповіщень спирається на те, що джерело зберігає `id` треку між опитуваннями. Це припущення про neptun.in.ua, не перевірене замірами | `src/lib/subscribers.ts` | обхід: поверх дедупу стоїть жорстка часова підлога `ALERT_FLOOR_MS` (4 хв), яку пробиває лише ескалація рівня. Замір стабільності id треба робити з розгортання |
| 41 | Прогноз руху хвилі веде ціль по прямій за останнім відомим курсом і типовою швидкістю типу. Цілі маневрують, а курс у даних оновлюється рідко | `src/lib/channel-wave.ts` | свідома межа: горизонт обмежено 30 хвилинами, у пості рядок названо прогнозом («за курсом далі»), а не фактом |
| 42 | Канал оголошував «відбій», щойно переставав бачити цілі в OSINT. Наші дані — повідомлення спостерігачів, а не радар: ціль, якої ми не бачимо, не перестає летіти, і читач, що вийшов з укриття за постом, вийшов рано | `src/server.ts`, `src/lib/channel-wave.ts` | виправлено: відбій прив'язано до офіційного зняття тривоги в усіх областях хвилі (`src/lib/official-alerts.ts`), і лише якщо тривогу оголошували. Поки вона триває, живий пост прямо каже «це не відбій». Те саме в картці `/my` |
| 43 | Є області, де офіційна тривога триває добами: чекати на її зняття означало б не дати відбою ніколи | `src/lib/channel-wave.ts` | свідома межа: після 12 годин тиші хвиля закривається **мовчки**, без поста. Сказати «відбою не буде» нічим краще, ніж постом, який прочитають як відбій |
| 44 | Акустичні доклади читачів живуть у памʼяті процесу — редеплой їх стирає | `src/server.ts` | свідома межа: доклад живе 30 хвилин і після цього нічого не означає. Запис на диск коштував би більше, ніж дані, які застаріють раніше, ніж їх прочитають |
| 45 | Випередження сирени («ми попередили на N хв раніше») рахується від моменту нашого повідомлення до появи області в стрічці ubilling. Це час до ПУБЛІКАЦІЇ офіційної тривоги в агрегаторі, а не до ввімкнення сирени | `src/lib/pre-alert.ts` | відкрито — затримка самого агрегатора не заміряна. Тому число показується лише в діапазоні 1–30 хв, а поза ним не згадується взагалі |
| 46 | Коло рідних не має виходу з кола через інтерфейс — лише неявно, при переході в інше | `src/server.ts` | відкрито — `leaveCircle` у сховищі є й викликається при переході; окремої команди поки немає |
| 47 | Самозцілення вебхука звіряло ЛИШЕ адресу. Якщо вона збігалася, `setWebhook` не викликався ніколи — і бот назавжди лишався підписаним на той перелік `allowed_updates`, з яким його зареєстрували вперше. Через це inline-режим і жива геолокація були мертві в продакшні: Telegram просто не доставляв `inline_query` та `edited_message`, без жодної помилки | `src/server.ts`, `ensureWebhook` | виправлено: звіряється й перелік типів (`webhookUpdatesOk`, з тестами); переоформлення НЕ скидає чергу оновлень; `allowedUpdates` тепер видно у відповіді `/setup` і `/repair` |
| 48 | У вікні Telegram `navigator.geolocation` не працює: у власному WebView немає браузерного діалогу дозволу, тож виклик мовчки відхиляється, а часом не кличе жодного зворотного виклику — кнопка «Визначити за геолокацією» зависала в стані «визначення…» до перезавантаження | `src/components/PersonalThreatPanel.tsx` | виправлено: `LocationManager` Telegram (Bot API 8.0) з обов'язковим `init()`, строком на кожен зворотний виклик і падінням на браузер поза Telegram. Причина відмови показується окремо, для `denied` є кнопка `openSettings()` |
| 49 | Клавіатура із запитом точки лишалась висіти внизу чату й після того, як точку прийнято — читалась як «не спрацювало, тисни ще» | `src/server.ts`, `savePoint` | виправлено: `remove_keyboard` разом із підтвердженням |
| 50 | Кнопка «Оновити» без заданої точки показувала «натисніть кнопку нижче» РІВНО без кнопки: reply-клавіатуру неможливо вкласти в редагування повідомлення — Telegram приймає там лише inline-кнопки | `src/server.ts`, `handlePersonalPress` | виправлено: надсилається нове повідомлення з клавіатурою замість редагування |
| 51 | Єдиним видимим шляхом задати точку була reply-кнопка `request_location`. Вона показується на всіх клієнтах, але на компʼютері натискання не робить нічого — джерела координат там немає. Людина лишалась без способу задати точку й без пояснення | `src/lib/bot-personal.ts`, `src/server.ts` | виправлено: основним шляхом став вибір області inline-кнопками (`oblast-picker.ts`) — працює на будь-якому клієнті без дозволів; запит геолокації сховано за кнопкою «Точніше» й супроводжується інструкцією окремо для телефона й компʼютера (📎 → Локація) |
| 52 | Точність вибору області — центр області, тобто орієнтир на десятки кілометрів. Для радіуса 50 км це може бути точка за 120 км від людини | `src/lib/oblast-picker.ts` | свідома межа: сказано прямо в тексті після вибору, разом із тим, як задати точніше. Ширшої бази населених пунктів у TS немає — вигадувати координати не можна (AGENTS.md §4) |
| 53 | Причину «бот не реагує» доводилось вгадувати: перелік типів оновлень було видно лише через `/setup` із секретом в адресному рядку | `src/server.ts` | виправлено: `/stats` у боті показує адресу вебхука, типи оновлень, чого бракує, чергу й останню помилку доставки |
| 54 | Картка `/my` показувала цілі за 600–720 км як «іде на вас, ~240 хв»: `personalAssessment` застосовував радіус лише до ЛІЧИЛЬНИКА вхідних, а перелік `nearest` брав з усіх цілей країни | `src/lib/advisory.ts` | виправлено: радіус застосовується й до переліку; поза радіусом додано окремий рядок «найближча за N км», щоб «у радіусі нічого» не читалось як «у країні нічого» |
| 55 | ETA рахується по прямій за курсом, який джерело оновлює рідко. «~240 хв до вас» виглядає точніше, ніж будь-що, що ми знаємо: ціль до того часу кілька разів змінить курс | `src/lib/bot-personal.ts` | виправлено: за годиною число не показуємо взагалі — «далеко, понад годину» |
| 56 | `/stats` показував «Усього: 0» посеред активного користування: відкладений на 2 с запис + перезапуск контейнера стирали щойно задану точку, а на зупинці процесу запису не було взагалі | `src/lib/subscriber-store.ts` | виправлено: поява підписника й нова точка пишуться негайно (з очікуванням), додано запис на SIGTERM/SIGINT/beforeExit |
| 57 | Негайний запис зробив перекриття `flush` не теоретичним: два дотики поспіль писали ОДИН тимчасовий файл, перший його перейменовував, другий падав на ENOENT — частина змін не доходила до диска | `src/lib/subscriber-store.ts` | виправлено: записи стоять у черзі, тимчасове імʼя унікальне, шлях фіксується один раз на запис. Зловлено новим тестом, не міркуванням |
| 58 | Рядок змін у каналі читався навпаки до змісту: частини склеювались через «·», тож зелена ✅ «цілей не бачимо» опинялась поруч із назвами областей, де цілі щойно ЗʼЯВИЛИСЬ, і люди розуміли рядок як «там чисто». Плюс «🆕» Telegram малює бейджем NEW, який нічого не каже українською | `src/lib/channel-lexicon.ts`, `channel-post.ts` | виправлено: кожна зміна окремим рядком, словами замість значків («Зʼявились цілі» / «Цілей більше не бачимо»), зелену галочку прибрано — вона читається як відбій |
| 59 | Гейт підписки на канал: перевірка членства залежить від того, що бот — адміністратор каналу, і що канал заданий як `@username` | `src/lib/gate.ts` | свідома межа: гейт відмовляє У БІК ДОСТУПУ (не перевірили — пустили), бо це інструмент безпеки; поза гейтом лишаються `/stop`, `/help`, `/status` і пауза сповіщень, а вже налаштована розсилка не вимикається заднім числом |
| 60 | Сховище підписок вимагало ВРУЧНУ заданого `BOT_DATA_DIR`, хоча Railway сам виставляє `RAILWAY_VOLUME_MOUNT_PATH`, щойно том підключено. Через цей зайвий крок том міг бути підключений, а підписники все одно зникали при редеплої | `src/lib/subscriber-store.ts` | виправлено: шлях береться з `RAILWAY_VOLUME_MOUNT_PATH` автоматично (перевірено в документації Railway), `BOT_DATA_DIR` лишився як перекриття |
| 61 | «Постійний том» був висновком із НАЯВНОСТІ ЗМІННОЇ, а не заміром. Том можна підключити не в ту теку, змонтувати лише для читання або впертись у квоту — конфігурація виглядає правильною, поки дані зникають | `src/lib/subscriber-store.ts` | виправлено: `probeWritable()` пише й прибирає пробний файл; `/stats` показує результат справжньої спроби запису окремо від висновку про том |
| 62 | Підключити том на Railway з телефона неможливо: документація дає лише ⌘K або правий клік по канвасу | Railway / розгортання | обхід у коді: щоденна резервна копія підписників іде файлом в особистий чат власника (`/backup`), відновлення — переслати файл назад. Це не заміна тому, а страховка від класу відмов, від якого том і не рятує |
| 22 | Пост каналу йде підписом до фото (стеля 1024 симв.), і перевищення різалося тупо — лишались шапка й підвал. У живому каналі: «🚀 Ракетна небезпека! всього в небі: 14» без жодної названої області. Вада зворотна за знаком: що більший наліт, то менше пост повідомляє | `src/server.ts` (`captionFor`), `src/lib/channel-post.ts` | виправлено: `assemblePost` жертвує за пріоритетом (хештеги → прогноз → зміни → найспокійніші області зі згорткою «…і ще N»); шапка й підвал лишаються завжди |
| 23 | Гейт підписки був ВИМКНЕНИЙ у проді: `TELEGRAM_CHANNEL_ID` задано числом, з якого `channelUrl` посилання не робить і повертає `null`, а `subscriptionGate` на `null` пускає всіх | `src/lib/gate.ts`, `src/server.ts` | виправлено: адресу питаємо в Telegram (`getChat` → `username` / `invite_link`), а не виводимо з налаштування. Заміряно: зі змінної `null`, із Telegram `https://t.me/Info_Radar` |
| 24 | `telegramSend` не обробляв 429: повідомлення писалося в лог і зникало, а обхід продовжував слати в тому ж темпі. Плюс `fetch` без строку — одне зависле зʼєднання зупиняло ВСІ тривоги до перезапуску (обхід захищений прапорцем від перекриття) | `src/server.ts` | виправлено: очікування за `retry_after` з тіла відповіді + один повтор; строк 15 с на виклик |
| 25 | Ціль над містом оголошувалась як така, що в це місто летить: «м. Київ: 2 шахеди — у бік: м. Київ (4–14 хв)». Поріг «вже над містом» був 3 км — великі міста ширші в рази | `src/lib/channel-post.ts` (`loudCity`) | виправлено: поріг 12 км |
| 26 | Тавтологія «Полтавщина — у бік: Полтавщина»: групування й довідник напрямків користуються ОДНИМ переліком обласних центрів, тож група й ціль часто збігаються | `src/lib/channel-post.ts` | виправлено: відомість не викинуто (людям у центрі важливо), а названо як є — «— на обласний центр (12–30 хв)» |
| 27 | HTML-оболонка віддавалася без жодного заголовка кешування, а ассети — `immutable` на рік. WebView Telegram тримав стару оболонку, та просила старі хеші, і людина бачила стару збірку нескінченно; жоден редеплой цього не лікував | `src/server.ts` | виправлено: оболонка `no-cache, must-revalidate`, ассети лишились `immutable`. Заміряно на проді |
| 28 | Покриття укриттями поза Києвом і Харковом мізерне: Запоріжжя 3 точки, Одеса 16, Львів 20 на місто-мільйонник. Це межа відкритої карти, а не помилка коду — але інтерфейс має її називати, і називає | `src/data/shelters.json` | відкрито: потрібне джерело державного реєстру захисних споруд |
| 29 | Імена в колі йшли в HTML-повідомлення БЕЗ екранування: `circle.ts` не мав жодного `escapeHtml`. Три вектори відтворено — чуже посилання в чаті рідних, підроблений текст від імені бота і (найгірше) зламана розмітка, через яку Telegram відхиляє повідомлення ЦІЛКОМ, тобто «я в порядку» не доходить | `src/lib/circle.ts`, `src/server.ts` | виправлено: екрановано розсилку відмітки, перелік кола, назву при вході, код запрошення |
| 30 | Горизонтальна прокрутка сторінки на 320 px: підпис плеєра часу мав `w-32 shrink-0` — 128 px, які не стискаються нізащо. Заміряно в браузері: scrollWidth 360 проти clientWidth 320 | `src/components/TimelinePlayer.tsx` | виправлено: `max-w-32 flex-shrink truncate`; перевірено на 320/360/390/768/1280 px |
| 31 | Невідомий тип цілі валив `renderChannelPost` ЦІЛКОМ: таблиці `Record<ThreatType, …>` припускали, що ключ завжди свій, а типи приходять із мережі. Одна дивна ціль позбавляла поста весь канал — у продукті, де мовчання і є найгіршою поразкою. Знайдено перебором граничних входів (11 025 викликів, 245 падінь) | `src/lib/air.ts`, `src/lib/channel-lexicon.ts` | виправлено: `normalizeThreatType` на межі + подвійний запобіжник у лексиконі; після зміни 0 падінь із тих самих 11 025 |
| 63 | Обхід підписників був O(підписники × цілі): відстань від кожного до кожної цілі щопівтори хвилини. На мільйоні підписників — 100 млн обчислень за такт, і майже всі марні наперед (наліт стоїть над кількома областями) | `src/server.ts`, `personalAlertSweep` | виправлено: просторовий індекс рівнями радіуса (`geo-index.ts`). Заміряно на 1 000 000 підписників і хвилі з 18 цілей: 593 → 229 мс (+179 мс побудова), кандидатів 188 831 замість 1 000 000, знайдено ті самі 65 169. Тест звіряє збіг із повним перебором |
| 64 | Індекс програє повному переборові, коли цілі стоять по всій країні (заміряно: 1 224 → 2 784 мс на синтетичному гіршому випадку) | `src/lib/geo-index.ts` | свідома межа, названа в коді: у цьому випадку вузьке місце вже не процесор — мільйон сповіщень на документованих 30/с це дев'ять годин |
| 65 | Розсилка мовчки відкидала всіх після 400-го за обхід, а порядок визначався тим, як підписники лежали у файлі: хто записався пізніше, того й попереджали пізніше — незалежно від того, кому в укриття | `src/server.ts` | виправлено: черга з пріоритетом і строком придатності (`delivery.ts`). Хвіст, на який бюджету немає, рахується як втрата й потрапляє в лог, а не зникає |
| 66 | Telegram документує ~30 повідомлень/с на розсилку: 100 000 підписників — 56 хвилин на одне сповіщення. Ніщо в системі про цю стелю не повідомляло | `src/lib/capacity.ts` | названо числом: `/stats` показує стелю й скільки людей НЕ встигаємо попередити в корисному вікні, з обома справжніми варіантами (платні розсилки або перенесення навантаження в канал). Мовчить, поки запасу вистачає |
| 67 | «Чи летить на мене» відповідалось перевіркою сектора ±60°: ціль, що промине за 30 км, вважалась вхідною так само, як та, що йде в лоб. Звідси половина зайвих сповіщень | `src/lib/advisory.ts` | виправлено: задача про найближче зближення (`approach.ts`) — рахується промах і шанс пройти ближче за радіус тривоги. Радіус спостереження і радіус тривоги розділені |
| 68 | Курс і швидкість бралися з поля джерела й таблички типів, без жодної міри похибки: «~22 хв» виглядало як вимір, хоч могло означати «від 14 до 35» | `src/lib/track-filter.ts` | виправлено: рух виводиться з послідовних фіксів, а розкид між ними дає похибку — заміряну на самих даних, не налаштовану константою |
| 69 | Канал вів власну історію треків для картинки, бот не вів жодної: два споживачі того самого факту з різною памʼяттю | `src/server.ts` | виправлено: спільна памʼять треків, оцінка руху рахується один раз на опитування, а не на кожного підписника |
| 70 | Точність оцінки руху обмежена джерелом: позиції з точністю до населеного пункту, з частотою в хвилини, від людей, які побачили й встигли написати | середовище / джерела | свідома межа, названа в коді: кращу оцінку дає не кращий алгоритм, а радар або ADS-B — інший клас даних |
| 71 | Дві сесії незалежно зробили той самий крок: оцінку руху зі спостережених фіксів. `trajectory.ts` (підгонка найменшими квадратами, R², рій, досягнуті пункти) і `track-filter.ts` (походження курсу, розворот, розклад невизначеності) — два способи рахувати те саме, тобто рівно те, що забороняє AGENTS.md §7 | `src/lib/` | зведено: підгонку вектора робить ОДНА реалізація — `trajectory.estimateVelocity` (математично краща: підгонка складових знімає задачу про середнє кутів узагалі). У `track-filter` лишився шар рішень, якого підгонка не дає: відсів помилок ототожнення per-type, походження курсу, кутова швидкість розвороту й розклад похибки на «вздовж»/«впоперек» для `approach.ts`. Мертве після зведення прибрано |
| 72 | Бот рахував траєкторії й шанси, але жодного разу не казав людині найпростішого — «у вашій області оголошено тривогу». Подію бачили щохвилини й використовували для відбою в каналі, а до того, кого вона стосується, не доносили | `src/server.ts` | виправлено: `oblast-watch.ts` + обхід офіційних тривог. Перший знімок навмисно не дає переходів — інакше кожен редеплой розсилав би «оголошено тривогу» тим, у кого вона вже тривала |
| 73 | Радар знав одну точку. Людина спить удома, працює за 20 км і хвилюється за матір в іншій області — і радар мовчав про все, крім обраного, ще й НЕПОМІТНО | `src/lib/saved-places.ts` | виправлено: до 5 іменованих місць, сповіщення підписані назвою. Головне місце дублюється в `point`, тож увесь код, що вміє «точку людини», працює без змін |
| 74 | Повідомлення про зліт носіїв (МіГ-31К, Ту-95) розчинялись у переліку цілей як «✈️ 1 борт»: найважливіше попередження виглядало як найменш важливе | `src/lib/carriers.ts` | виправлено: окреме попередження «готовність по країні» з прямою вказівкою, що пуску ще НЕ БУЛО. Тип `aircraft` сам по собі замалий — потрібен ще й текст носія, інакше попередження знеціниться щоденним повторенням |
| 75 | Точність оцінки маршруту: пряма замість дороги, стала швидкість, теперішня обстановка замість прогнозу на час прибуття | `src/lib/route.ts` | свідома межа, названа в самому тексті відповіді. Далі потрібні маршрутизація (OSRM/Valhalla) і прогноз на годинний горизонт — дані, яких немає |

| 76 | Дві сесії незалежно зробили ті самі дві сутності: «мої місця» (`saved-places.ts` проти `places-mine.ts`) і «моя статистика» (`diary.ts` проти `personal-stats.ts`). У `Subscriber` через це опинилось ДВА поля `places` різних типів — код не збирався б, а зібравшись, дав би людині два переліки місць, з яких попереджав би лише за одним | `src/lib/`, `src/server.ts` | зведено по одному на сутність. Місця: лишився `places-mine` (має кулдаун, поріг серйозності для чужого місця й підпис «за що саме прийшло»), з `saved-places` перенесено власний радіус місця (`/place дача Ірпінь 30`) — навколо дачі поле, навколо дому місто. Статистика: лишилась `personal-stats`, але зберігання переведено з місяців на ДОБИ — місяць виводиться з діб, доби з місяця ні, і саме доби дають тиждень, «скільки ночей розбудили» й найгучнішу добу. Середнє випередження рахується сумою й лічильником замість масиву останніх 20: менше місця і точніше. Команди `/diary` й `/month` були одним питанням — лишився `/month` |
| 77 | Перелік команд жив у ДВОХ місцях: меню Telegram (`publicCommands`) і текст `/help`. Вони вже розійшлися — у меню були `/place`, `/month`, `/calm`, `/weather`, у довідці їх не було. Людина шукала команду там, де їй сказали шукати, і не знаходила половини бота; жоден тест цього не ловив, бо обидва тексти окремо були правильні | `src/lib/telegram.ts` | виправлено: `/help` будується з того самого переліку, довший рядок за потреби дає необовʼязкове поле `hint`. Тест звіряє, що кожна команда меню є в довідці |
| 78 | Накладки над картою позиціювали себе САМІ: `bottom-24`, `bottom-[46px]`, `left-14 top-2` (двічі — у двох різних панелей однакові координати), `bottom: 3rem` в CSS для атрибуції. Кожне число було припущенням про сусідів «плеєр заввишки 46 px», «реплей точно є». Щойно припущення не справджувалось — блоки налазили один на одного, і підправлене число ламало наступну накладку. Саме тому дефект повертався після кожного виправлення | `src/components/MapOverlays.tsx`, `src/routes/index.tsx`, `src/styles.css` | виправлено структурно: накладки не позиціюються взагалі, а лежать у потоці всередині смуг — потік накладати елементи не вміє. Відступ під смуги міряється (`--map-bottom-inset`) і піднімає атрибуцію Leaflet рівно на її висоту; бічні відступи беруться з `--map-ctrl-*`, заміряних там, де ті елементи стилізуються. Заміряно в браузері на 320/360/390/430/768/1280 px із розгорнутою легендою і без: 0 перетинів, 0 горизонтальної прокрутки, атрибуція в межах карти в усіх 12 випадках |
| 79 | `h-full` (потрібний, щоб легенда могла обмежитись висотою карти) і `flex-wrap` (потрібний, щоб чип областей переносився) окремо правильні, а разом ламаються: у перенесеному рядку `h-full` рахується від висоти КОНТЕЙНЕРА, а не свого рядка. Заміряно: колонка чипа звисала на 343 px нижче за карту | `src/components/MapOverlays.tsx` | виправлено: переносу в цій смузі немає взагалі — легенда й чип мають окремі рядки, тож чип дістає всю ширину (саме брак ширини й різав назви областей) |
| 80 | Панель активних хвиль не мала стелі висоти: на великому нальоті (заміряно на 136 шахедах) вона виростала нижче за карту й накривала плеєр часу | `src/components/MapOverlays.tsx`, `ActiveWaves`, `WaveForecast`, `PersonalThreatPanel` | виправлено: верхня смуга має визначену висоту до нижніх смуг (з тієї ж виміряної `--map-bottom-inset`), панелі в ній обмежені `max-h-full` і прокручуються всередині |
| 81 | Двічі поспіль виправлення розкладки «не спрацьовувало»: на скріншотах був напис, прибраний з коду добою раніше (`bc7c269`, 15.09 17:09). Тобто дивились на стару збірку — і це неможливо було ні підтвердити, ні спростувати, бо застосунок ніде не казав, з чого зібраний. Час ішов на пошук вади, якої в коді вже не було | `src/lib/build-info.ts`, `src/components/StaleShellNotice.tsx`, `src/server.ts` | виправлено: оболонка несе мітку збірки (`<meta name=x-build>`), `/api/build` віддає поточну живим запитом без кешу, розбіжність між ними і є доказом застрягання — тоді консоль прямо каже «ви бачите стару версію». Без однієї з міток мовчить: «усе свіже» без підстав — те саме тихе неправдиве твердження, проти якого перевірка й заводиться. `/stats` показує мітку боту. Перевірено в браузері в 4 станах |
| 82 | **Прод замерз на добу, і жодне виправлення до людей не доходило.** 15.09 о 17:19 у `package.json` додали `playwright`, але `bun.lock` не оновили. `bun install --frozen-lockfile` падає на розбіжності — а bun вмикає `--frozen-lockfile` САМ, коли в оточенні `CI=true`. Тому впала і GitHub Actions, і збірка Railway: 219 прогонів CI, усі свіжі — червоні, і остання жива збірка на проді датується коміту ПЕРЕД тим (15.09 17:09). Заміряно на проді: `/api/build` → 404, в HTML лежить напис, прибраний з коду добою раніше | `bun.lock` | виправлено: лок приведено у відповідність. Клас вади — не в коді, а в тому, що про зупинку деплою ніщо не повідомляло: три сесії писали код, який фізично не міг доїхати, і шукали вади в тому, що бачили на екрані |
