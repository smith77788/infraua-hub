# Відкриті знахідки

Проблеми, знайдені, але ще не виправлені. Один рядок — одна знахідка.
Стан міняється в тому ж коміті, що й саме виправлення (див. `AGENTS.md`, §3).

Стани: `відкрито` · `в роботі (хто)` · `виправлено (коміт)` · `відхилено (чому)`

| # | Знахідка | Де | Стан |
|---|---|---|---|
| 1 | Верхня панель обрізана на вузькому екрані: «Оновити», «Брифінг», «Про платформу» виходять за межу й недосяжні | `src/routes/index.tsx` (шапка) | відкрито |
| 2 | Підпис карти «Leaflet \| Tiles © Esri» перекриває плеєр часу на телефоні | `src/styles.css`, шар підпису Leaflet | виправлено (підпис піднято над плеєром, контроли Leaflet у темі консолі) |
| 3 | Кількість підстанцій 110 кВ+ по країні | `src/lib/infra.functions.ts` | виправлено: заміряно з розгортання — **4109** (3 вузли, 3937 ліній, 169 відношень). Причиною відмов Overpass був відсутній `User-Agent`, а не недоступність |
| 4 | Розходження гідратації: час брався під час першого малювання | `TimelinePlayer`, `routes/brief.tsx` | виправлено (b8101f0) |
| 5 | Вигадана адреса `hello@infraua.org` на сторінці «Про платформу» | `src/routes/about.tsx` | виправлено (b8101f0) |
| 6 | `vitest` падає на всіх файлах — не той рушій, а не поламані тести | README | виправлено (b8101f0) |
| 7 | Опорні записи мали `source` виду `openstreetmap.org/#map=…` — вид карти, а не запис; у інтерфейсі не відрізнялися від перевірених | `infra-seed.ts`, інспектор | виправлено (поле `origin`, кнопка «Джерело» лише за наявності запису) |
| 8 | Запити до Overpass ішли без `User-Agent`, що порушує політику OSM; відмови (HTTP 406) читалися як «джерело лежить» | `src/lib/infra.functions.ts` | виправлено (A/B з Railway: 406 → валідний JSON) |
| 9 | З цього середовища розробки Overpass недосяжний на рівні зʼєднання (HTTP 000) — заміри з ним робити лише з розгортання | середовище | відкрито |
| 10 | `platform-api` на Railway не має тригера автодеплою: `deploymentTriggerCreate` недоступний project-токену. Розгортається вручну (`latestCommit: true`) | Railway | відкрито — потрібна дія власника в інтерфейсі |
| 11 | Дані димового тесту в графі платформи | `platform-api` | виправлено: додано відкликання партії (`POST /api/platform/retract`), партію `smoke` відкликано, граф порожній, дію записано в аудит |
| 12 | `AuditLog` тримав весь журнал у памʼяті; з аудитом читання він росте разом із трафіком | `platform/core/audit/AuditLog.ts` | виправлено: у памʼяті лишається покажчик (зсув + інтерновані актор/дія), записи читаються з диска. Заміряно на 100 000 записів (32.6 МБ): утримувана памʼять 90.0 → 11.9 МБ; ціна — старт 185 → 255 мс, `verify` 336 → 599 мс |
| 13 | П'ять пакетів (`leaflet`, `react-leaflet`, `@react-leaflet/core`, `@types/leaflet`, `@types/geojson`) закріплені в `bun.lock` на приватне дзеркало Lovable `europe-west1-npm.pkg.dev`, недосяжне з середовища розробки Claude Code | `bun.lock` | обхід: ті самі тарболи з `registry.npmjs.org` — sha512 збігається з lock-файлом побайтово, перевірено |
| 14 | `findDuplicateCandidates` порівнював кожну пару в межах типу на кожен запит `/analytics`: 500 вузлів — 1.8 с, 1000 — 7.6 с, 2000 — 29 с, 4000 — 116 с | `platform/core/analytics/EntityResolver.ts` | виправлено: блокування перед порівнянням, 4000 вузлів — 0.2 с; тест доводить збіг із повним перебором |
| 15 | `VectorIndex.search` перевекторизовував увесь корпус на кожен запит: 5000 документів, 20 запитів — 913 мс | `platform/core/vector/VectorIndex.ts` | виправлено: інвертований індекс і збережені норми, ті самі 913 → 95 мс; тест доводить побітову рівність ранжування |
| 16 | Список корпоративних суфіксів у `normalizeLabel` — лише латиниця, а `\b` у JS визначено через ASCII `\w`, тож кириличний суфікс не міг би зіставитися навіть після додавання | `platform/core/analytics/EntityResolver.ts` | виправлено: фільтрація цілих токенів, додано українські й російські правові форми |
| 17 | Токенізатор пошуку — `[a-zа-яё0-9]`, тобто російська абетка: і, ї, є, ґ ставали роздільниками. «Київобленерго» індексувалося як «ки»+«вобленерго», «Дніпро» — як «дн»+«про». Той самий клас помилки, що виправлений у `slugify` | `platform/core/vector/VectorIndex.ts` | виправлено: `\p{L}\p{N}` з прапорцем `u`, додано українські стоп-слова |
| 18 | На розрідженому графі обидва структурні сигнали ризику вироджувалися: у лісі мостом є кожне ребро, а посередництво нормується на максимум у знімку. Заміряно на 100 справжніх тендерах: 138 зі 164 сутностей мали бали, кілька — «severe» | `platform/core/analytics/RiskScorer.ts`, `GraphMetrics.ts` | виправлено: `minSide` для мосту (розмір меншої сторони), `minRaw` для посередництва (абсолютна кількість шляхів) |
| 19 | Половина онтології (`AWARDED_CONTRACT`, `HIDDEN_BENEFICIARY_OF`) і найважчий сигнал ризику (`self_dealing_pattern`, вага 35) не мали жодного джерела даних | `platform/config/` | виправлено: Prozorro (договори) + ЄДР (засновники, задекларовані бенефіціари, керівники). `HIDDEN_BENEFICIARY_OF` **навмисно** лишився без джерела: реєстр тримає розкрите володіння, і підключати його до «прихованого» означало б оцінювати законослухняність як приховування |
| 20 | Bun `fetch` через проксі середовища розробки падає з `ECONNRESET` на `public.api.openprocurement.org`; `curl` і Python до того самого хоста працюють | середовище | відкрито — живі заміри робилися через Python, фікстура зі справжніх записів у `platform/tests/fixtures/` |
| 21 | Відтворення минулого стану програє весь журнал ревізій: вартість росте з історією, а не з розміром відповіді | `platform/core/graph/RevisionLog.ts` | відкрито — відповідь тут періодичні знімки, а не інша побудова; поки журнал малий, це не болить |
| 22 | Відмітка часу не розрізняє дві зміни в одну мілісекунду, а завантаження робить їх сотнями | `platform/core/graph/` | виправлено: курсор послідовності (`asOfSeq`, `revisionHead`); відмітка часу лишилася для людських запитів |
| 23 | Наслідки відмови рахуються в обʼєктах і мегаватах, але не в людях. Потрібне населення: теги `place=*` з `population` в OSM дають його по населених пунктах | `src/lib/contingency.ts` | відкрито — Overpass із середовища розробки недосяжний (№9), тож підключати й міряти це треба з розгортання |
| 24 | Перебір епіцентрів бере кандидатами самі обʼєкти набору: епіцентр між двома обʼєктами, що зачепив би обидва, не зʼявиться | `src/lib/contingency.ts` | відкрито — свідома межа методу, названа в коді; рівномірна сітка по країні витратила б більшість роботи на порожні квадрати |
| 25 | Ні Node, ні Bun не декодують windows-1251, у якому публікується ЄДР; `TextDecoder` кидає `ERR_ENCODING_NOT_SUPPORTED`, а обхід через UTF-8 не помиляється й дає кашу | `platform/core/ingestion/EdrConnector.ts` | виправлено: власний декодер, таблиця згенерована з еталонного кодека і звірена в тестах |
| 26 | `self_dealing_pattern` вимагає обох звʼязків на **одному** вузлі, а справжній візерунок конфлікту інтересів — це шлях: особа є бенефіціаром постачальника і керівником замовника, який йому присудив | `platform/config/risk_signals.json` | виправлено: `core/analytics/ConflictOfInterest.ts` + `GET /api/platform/analytics/conflicts`. Сам сигнал лишено для випадку, коли обидві ролі тримає організація, і межу названо прямо в його `reason` |
| 27 | Тест стверджував, що `new TextDecoder('windows-1251')` кидає — це властивість рантайму, а не коду. Локально Bun 1.3.11 кидає, у CI `bun-version: latest` уже вміє, і CI впав на `main`, поламавши й коміти паралельної сесії | `platform/tests/integration/edr.test.ts` | виправлено: перевіряється поведінка конектора, а не рантайму; звірка з платформним декодером робиться там, де він є, і пропускається, де немає |
| 28 | Таблиця cp1251 бралася з кодека Python, який вважає байт 0x98 невизначеним, тоді як WHATWG (і будь-який `TextDecoder`) відображає його в U+0098 — звірка двох декодерів впала б на новому рантаймі | `platform/core/ingestion/EdrConnector.ts` | виправлено: таблиця з індексу WHATWG, звірено побайтово |
| 29 | Стан джерел у консолі бачить лише поточне звернення: джерело, що віддає 200 із третиною звичних рядків, не «лежить», але зсуває кожне похідне число | `src/lib/sources.ts` | частково: у платформі зʼявилися знімки партій із вироком (`platform/core/lineage/`). Консоль ще їх не читає — це вимагає рішення щодо подання |
| 30 | Журнал ревізій графа так само програється цілком, і `RevisionLog` тримає всі ревізії в памʼяті — та сама проблема, що була у №12, лише повільніша | `platform/core/graph/RevisionLog.ts` | відкрито — той самий підхід (покажчик замість обʼєктів) плюс періодичні знімки |
