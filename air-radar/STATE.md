# Platform State & Architecture Log

_Автономний журнал: рішення, перевірені факти, гіпотези, наступні цілі._

## Стратегічний контекст
Проєкт = єдина situational-awareness платформа для критичної інфраструктури
України + повітряних загроз. Було: лише клієнтський фронтенд (Lovable/edge),
без бекенду, персистентності, серверного NLP, тестів. **Крупнейший gap —
відсутність платформного бекенду.** Цей модуль (`air-radar/`) його закриває.

## Перевірені ФАКТИ (експериментально підтверджено в цьому середовищі)
- GeoNames `UA.zip` доступний; побудовано гео-базу: **104 213** унікальних
  нормалізованих назв, 162 220 записів (SQLite). Пошук у тексті + склонення
  працюють (Полтаву→Полтава, Дніпра→Дніпро, Харкові→Харків).
- `detoyshahed.in.ua/api/incidents/active` та `/alerts/active` — **keyless**,
  віддають реальні позиції цілей (EPSG:3857) і полігони тривог (WGS84).
- FastAPI-сервіс піднімається, WS `/ws` віддає snapshot: перевірено **93
  реальні обʼєкти**, 50 зон, 80 логів; дельти стрімляться; SQLite накопичує
  треки (267 за 10 хв). Статика COP віддається (200).
- pytest: **10/10** (нормалізація, склонення, азимут/дистанція, детект типу,
  екстракція маршруту, дедуп id, побудова обʼєкта).

## Розроблені всередині проєкту механізми (не зовнішні бібліотеки)
- Гео-нормалізатор + евристики українського відмінювання (о↔і чергування).
- Екстрактор типів/маршрутів/векторів на лексиконах+RegEx (без ML-ваг).
- Стабільний content-hash id для дедуплікації та оновлення треків.
- WS-брокер з дельтами + фоновим прибиранням прострочених обʼєктів.

## Гіпотези / обмеження (потребують перевірки або зовнішнього ресурсу)
- Bridge-обʼєкти мають тип `unknown` (джерело не віддає сирий текст) — повна
  класифікація типів зʼявиться на Telethon-потоці. **HUMAN ACTION: ключі TG.**
- ETA/вектор точні лише коли є маршрут із ≥2 точок або «курс на X».
- Точність склонення — евристична; рідкісні форми можуть не резолвитись.

## Наступний STRATEGIC OBJECTIVE
Підняти платформу з «сервіс з даними» до «інтелектуальна система»:
1. **Track fusion & motion model** — обʼєднання повторних сповіщень в єдиний
   трек з екстраполяцією позиції за курсом/швидкістю між оновленнями.
2. **Threat→asset correlation** — звʼязати повітряні цілі з обʼєктами
   інфраструктури (спільна модель даних із InfraUA-консоллю).
3. **Unified data plane** — спільний бекенд для InfraUA-консолі та радара
   (одна модель, одне сховище, один WS-стрім).
4. **Eval/observability** — метрики якості екстракції, health, alerting.
